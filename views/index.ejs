<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Modern Mountain Sounds</title>

        <!-- skeleton framework -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css">

        <!-- partials -->
        <%- include ('./partials/head.ejs') %>
        <%- include ('./partials/header.ejs') %>

    </head>
    <body>
        <br>
        <h4>We strive to bring the names of progressive artists and bands to the foreground and celebrate the various countries, races, and experiences these artists bring to the table.</h4>
        <br>

        <nav>
            <a style="background-color:#FABDA5;color:#434D28;" class="button" href="/index/new">Add a Band</a>
        </nav>

        <table class="u-full-width">
            <thead>
                <tr>
                    <th>Image</th>
                    <th>Name</th>
                    <th>Would you like to...</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < bands.length; i ++) { %>
                    <tr>
                        <!-- imgage and name -->
                        <td><img style="width:500px;height:auto;" src="<%=bands[i].image%>"</td>
                        <td>
                            <h5><%=bands[i].name%><h5>
                        </td>

                        <!-- view/delete buttons -->

                        <!-- view -->
                        <td>
                            <form action="/index/<%=bands[i]._id%>" method="GET">
                                <input style="background-color:#F29F05;" class="button" type="submit" value="View Band">
                            </form>
                        </td>

                        <!-- delete -->
                        <td>
                            <form action="/index/<%=bands[i]._id%>?_method=DELETE" method=POST>
                                <input style="background-color:#733614;color:white;" type="submit" value="Delete Band">
                            </form>
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </body>
</html>
